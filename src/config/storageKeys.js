/**
 * 统一的 localStorage 键名常量
 * 所有持久化存储键都在这里定义，便于管理和查找
 */

// AI 设置（baseUrl, apiKey, 模型名, 提示词等）
export const AI_SETTINGS_STORAGE_KEY = 'aiSettings';

// 配色方案 ID（0-7）
export const COLOR_SCHEME_STORAGE_KEY = 'colorScheme';

// 用户反馈身份（anonymousId, contributorNickname）
export const USER_IDENTITY_STORAGE_KEY = 'userID';

// 搜索历史记录
export const SEARCH_HISTORY_STORAGE_KEY = 'searchHistory';

// 用户语言偏好
export const LANGUAGE_STORAGE_KEY = 'languagePreference';
